# 02_组件规范

> 记录当前项目已经存在或明确约定的可复用 UI 组件，不重复通用组件拆分与职责原则

## 一、组件登记

| 组件名称 | 类型 | 场景路径 | 主要用途 | 可复用范围 |
| --- | --- | --- | --- | --- |
| `[组件名称]` | `[按钮 / 面板 / 弹窗 / 列表项 / 其他]` | `[路径]` | `[用途]` | `[全局 / 指定模块]` |

## 二、按钮组件

1. 基础按钮组件：`[组件名称 / 场景路径]`
2. 已有样式：
   1. `[普通]`：`[Variation / 配置方式]`
   2. `[高亮]`：`[Variation / 配置方式]`
   3. `[警示]`：`[Variation / 配置方式]`
3. 支持内容：`[纯文字 / 图标+文字 / 纯图标]`
4. 特殊按钮：`[如无则删除]`

## 三、面板组件

1. 基础面板组件：`[组件名称 / 场景路径]`
2. 主要用途：`[普通容器 / 信息卡片 / 功能区域 / 其他]`
3. 已有变体：
   1. `[变体名称]`：`[用途]`
   2. `[变体名称]`：`[用途]`

## 四、弹窗组件

1. 基础弹窗组件：`[组件名称 / 场景路径]`
2. 标准结构：`[标题 / 内容 / 按钮区 / 关闭按钮等]`
3. 已有弹窗类型：
   1. `[确认弹窗]`：`[用途]`
   2. `[提示弹窗]`：`[用途]`
   3. `[自定义弹窗]`：`[用途]`
4. 特殊关闭规则：`[如无则删除]`

## 五、列表项组件

1. 基础列表项组件：`[组件名称 / 场景路径]`
2. 使用场景：`[商店 / 背包 / 设置项 / 关卡列表 / 其他]`
3. 主要状态：`[默认 / 选中 / 禁用 / 已完成等]`
4. 特殊内容结构：`[如无则删除]`

## 六、文本与图标组件

1. 标题文本组件：`[组件名称 / 无独立组件]`
2. 正文文本组件：`[组件名称 / 无独立组件]`
3. 数值文本组件：`[组件名称 / 无独立组件]`
4. 图标按钮组件：`[组件名称 / 无独立组件]`
5. 其他固定组合：`[如无则删除]`

## 七、组件接口记录

| 组件名称 | 主要输入 | 主要输出 | 关键状态 |
| --- | --- | --- | --- |
| `[组件名称]` | `[文本 / 图标 / 数据 / 配置]` | `[点击 / 选择 / 关闭等]` | `[默认 / 禁用 / 选中等]` |

## 八、项目特殊组件约定

1. `[填写仅当前项目存在的组件约束]`
2. `[没有则删除本节]`

## 九、维护规则

1. 新增、删除或替换可复用组件时，需要同步维护本文件
2. 已有组件的用途、变体或接口发生变化时，需要同步维护本文件
3. 仅单个页面临时使用且无复用价值的 UI 结构，不写入本文件
